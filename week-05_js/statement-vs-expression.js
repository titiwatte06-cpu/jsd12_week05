// A statement does something.
// An expression produces a value.

// Example

let x = 5; // statement
3 + 2; // expression

// In JavaScript, expressions can often be used inside statements:

if (age > 18) {
  // `if` is a statement, `age > 18` is an expression
  console.log("Adult");
}
